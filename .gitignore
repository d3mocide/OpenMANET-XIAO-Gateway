build/
managed_components/
dependencies.lock
sdkconfig
sdkconfig.old

# Generated by .github/workflows/build-firmware.yml, not committed - see
# docs/index.html for the hand-written source. Per-region output lands in
# docs/<region>/ (e.g. docs/us/manifest.json).
docs/*/manifest.json
docs/*/*.bin
